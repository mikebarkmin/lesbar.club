import styled from 'styled-components';

const TextArea = styled.textarea`
  font-size: 1.2rem;
  font-family: serif;
  width: 100%;
  height: 100%;
  resize: none;
  border: none;
  min-height: 400px;
  overflow: visible;
  padding: 16px;
`;

export default TextArea;
